#include <stdio.h>

struct Person {
    char name[50];
    int age;
    float salary;
};

int main() {
    struct Person p1;

    printf("Size of structure: %lu bytes\n", sizeof(p1));

    return 0;
}
