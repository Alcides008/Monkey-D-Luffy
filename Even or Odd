#include <stdio.h>

int main () {
  int a;
  
  printf("Enter 1st no.\n");
  scanf("%d",&a);
  
  
  if (a%2 == 0) {
      printf("The no. is even %d \n", a);
  }
  
  else 
{
 printf("No. is odd %d \n" , a);
}
  
}
