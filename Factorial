#include <stdio.h>

int main()

{
    int num, i = 1, factorial = 1;
    printf("Enter a number: ");
    scanf("%d", &num);

    while (i != num + 1)
    {
        factorial = factorial * i;
        i = i+1;
    }
    
    printf("Factorial = %d", factorial);

    return 0;
}
